import { atom } from 'jotai';

export const tasksAtom = atom([]);
export const modalVisibleAtom = atom(false);
export const selectedTaskIdAtom = atom(null);
export const clearAllModalVisibleAtom = atom(false);
